// If this file is updated do:
// Run "npm install esbuild" If esbuild is not installed already
// Use "npx esbuild bundle.js --bundle --outfile=out.js"
// Add this line at the bottom of out.js: export const out = ""
// Then Move out.js to client/engine/extras

// TODO: Add a script that will auto build and move this to right place.

window.mediasoup = require('mediasoup-client')